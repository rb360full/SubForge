# Project Overview

SubForge is a subscription pipeline for V2Ray and Xray ecosystems.

Its job is to gather raw proxy configuration data from configured providers, turn that data into a normalized model, validate it, remove duplicates, test connectivity, filter unusable nodes, and publish final subscription files.

The project is intentionally structured for long-term maintainability.

