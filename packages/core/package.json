{
  "name": "@subforge/core",
  "version": "0.1.0",
  "private": true,
  "description": "Core parser and shared contracts for SubForge",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  }
}
